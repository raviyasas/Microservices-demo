spring:
  application:
    name: prolauncher-service
  cloud:
    config:
      uri: http://localhost:8888

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

file:
  source:
    path: C:\Users\RaviYasas\Desktop\files\Capture.zip